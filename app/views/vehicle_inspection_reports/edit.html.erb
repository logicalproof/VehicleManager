<h1>Vehicle Inspection Report for Vehicle #<%= VehicleServiceRecord.find(params[:vehicle_service_record_id]).vehicle.number %></h1>

<%= render 'form' %>

<%= link_to 'Show', @vehicle_inspection_report %> |
<%= link_to 'Back', vehicle_inspection_reports_path %>
